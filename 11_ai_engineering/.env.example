# SEC EDGAR requires a descriptive User-Agent containing a contact email.
# Requests without it are rejected. Format: "Name email@domain.com"
SEC_USER_AGENT=Your Name your.email@example.com

# Only needed for the paid provider (final evaluation runs).
# Leave empty while working locally with Ollama.
ANTHROPIC_API_KEY=
